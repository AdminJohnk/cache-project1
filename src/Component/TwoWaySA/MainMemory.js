export default class MainMemory {
    constructor(numberOfBlock, Array, currentIndex, byteOfBlock) {
        this.numberOfBlock = numberOfBlock;
        this.Array = Array;
        this.currentIndex = currentIndex;
        this.byteOfBlock = byteOfBlock;
    }
}